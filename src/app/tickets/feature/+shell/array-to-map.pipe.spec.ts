import { ArrayToMapPipe } from './array-to-map.pipe';

describe('ArrayToMapPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayToMapPipe();
    expect(pipe).toBeTruthy();
  });
});
